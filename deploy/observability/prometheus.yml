global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: linklytics-api
    metrics_path: /metrics
    static_configs:
      - targets: ['api:3001']
        labels:
          service: api
