{
  "name": "linklytics",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hostable URL shortener with click analytics. Monorepo: Next.js web + NestJS API, Postgres + Redis, Docker, Kubernetes (kind), CI/CD.",
  "license": "MIT",
  "packageManager": "pnpm@10.34.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "prettier --check \"**/*.{ts,tsx,js,mjs,cjs,json,md,yml,yaml}\"",
    "format:write": "prettier --write \"**/*.{ts,tsx,js,mjs,cjs,json,md,yml,yaml}\"",
    "compose:up": "docker compose -f deploy/compose/docker-compose.yml up --build -d",
    "compose:down": "docker compose -f deploy/compose/docker-compose.yml down -v",
    "compose:logs": "docker compose -f deploy/compose/docker-compose.yml logs -f",
    "compose:build": "docker compose -f deploy/compose/docker-compose.yml build",
    "k8s:up": "node scripts/k8s-up.mjs",
    "k8s:down": "node scripts/k8s-down.mjs",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "@swc/core",
      "esbuild",
      "protobufjs"
    ]
  }
}
