# pnpm config — keep installs deterministic and CI-friendly.
auto-install-peers=true
strict-peer-dependencies=false
# Hoist nothing implicitly we don't ask for; apps declare their own deps.
shamefully-hoist=false
# Speeds up CI; lockfile is the source of truth.
prefer-frozen-lockfile=true
