# Linklytics web — env template. Copy to `.env.local` for local dev.
# Browser-visible API origin. In docker-compose this is the API host port; behind
# the k8s ingress it is the ingress origin (same host as the web app).
NEXT_PUBLIC_API_URL=http://localhost:3001
